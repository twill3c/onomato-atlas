# AGENTS.md — onomato-atlas(音象アトラス)

近代日本語のオノマトペについて、**音側(音韻素性)と意味側(コーパス分布)を独立に測り、
一致した範囲だけを地図にする**プロジェクト。可視化は成果物であって主張ではない。
主張は「音の形が文脈を予測する」であり、これは反証可能でなければならない。

読む順: 本書 → `docs/concept.md`(構想と先行検証)→ SPEC.md → TEST_SPEC.md。

---

## プロジェクト固有規律

### 1. 循環の禁止(最重要・SPEC F-00)

**意味側の座標を音側の素性から計算してはならない。**

音側から座標を作ると「形態バリアントは持続軸だけ動く」「濁音は重い方へ動く」は
**構成上の恒真式**になり、何も検証できなくなる。これは v1 構想で実際に犯した誤りで、
`docs/concept-v1.md` に記録がある。

- 意味側はコーパスからのみ構成する
- 音側は**検証・色分け・逆引き**にのみ用いる
- 意味側の構成コードから音側モジュールを import しない(T-020 が静的検査する)
- **T-020 に落ちた変更は、他のテストが全緑でも不合格**

### 2. 語彙 curation は人手のみ

ABAB 型の採否は人間の判断で行う。品詞でも頻度でも擬態語は切り出せないことが実測済み
(`とうとう` `なかなか` `わざわざ` はすべて副詞 100%・高頻度 — docs/concept.md §2 実測 4)。

- エージェントが判定困難語を見つけた場合、**採用も除外もせず** `needs_review` に出力し、
  `escalation` を記録して人間に確認する
- 変化形(ABり/ABん/ABっ)は「語幹が ABAB 型として語彙内に独立に存在する」ことを
  要件とする自動規則で足りる(実測で偽陽性ゼロ)。**この規則を緩めない**
- 全採否判断は理由付きで `vocab_manifest.json` に記録する。理由が空の採否を残さない

### 3. 測定器の版管理

軽動詞ストップリスト・curation 除外リスト・対比語対の定義は**測定器の設計であり研究の一部**。
無断で動かさない。

- 各ファイルはヘッダに version を持つ。version の無い辞書を読んだら例外を投げる
- 変更は専用コミット(`data: stoplist vX.Y — 理由`)で行い、機能変更と混ぜない
- 変更したループでは**感度分析**(変更前後の座標変位・近傍の入れ替わり)を実行し、
  結果をループログに記録する

### 4. 較正ファーストと、通っていない軸を出さない規律

- 閾値・合格ライン・SVD 次元数を**較正の前に SPEC へ書かない**
- 軸は手で定義せず、対比ベクトルとして推定し、**検定(対照 2 = 対応シャッフル超え)を
  通ったものだけ**を UI に出す。通っていない軸はスライダーも射影値も提供しない
- 2026-08-25 時点の状態: O-1(形態パラダイム)**通過** / O-2(濁音)**未通過**。
  O-2 が通らないまま「濁音は重い」と UI に書いてはならない

### 5. 否定的結果を隠さない

期待した画が出なかったときに閾値を動かして通すことを禁じる。

- O-2 が拡大後も通らなければ「通らなかった」と表示する
- O-5(賢治造語)が既存語空間の外に落ちたら、外に落ちたまま公開する
- 実測が構想の予想を裏切った場合は `docs/concept.md` に**予想が外れた旨を明記**して残す
  (先行検証で実際に 2 件外れている: 品詞は散らなかった / 5 軸 UI は成立しなかった)

### 6. 引用の二層原則

テキストは**表示層(原文逐語)と分析層(正規化済み)の二層**で持つ。

- 用例の引用フィールドは青空文庫本文と**逐語一致**でなければならない(T-050)
- 正規化・要約・句読点変更を加えた文字列を「引用」として出力してはならない
- 引用には底本表記(底本・入力者・校正者)を必ず付す

### 7. 技術スタック規約

- パイプライン: Python 3.12。外部依存は **fugashi + unidic-lite + numpy のみ**(SPEC N-01)。
  係り受け解析器を導入しない(窓 3 以内に用言 77.1% 到達を実測済み)。
  追加が必要と判断したらエスカレーション
- Web: Next.js 静的 export。Root Directory = `web`。**v0.4 までサーバー実行を持たない**
- テストはフィクスチャ駆動。青空文庫への実アクセスは取得コマンドの手動実行のみ(N-02)
- 語彙数・抽出件数を**テストで定数固定しない**。件数オラクルは存在しない(SPEC §7)

### 8. ループ運用

- L1(コーパス・curation)完了までは SPEC の語彙規模・品質基準 Q-01/Q-02 は仮置き。
  実測による確定は専用コミットで行う
- 音側(F-10)と意味側(F-11)は契約(`phon.json` / `sem.json`)を境界に worktree 並走可
- `probes/` は先行検証のスクリプト。**本流のパイプラインではない**。
  ここから移植するときは規律(版管理・二層原則)を満たす形に書き直す
- **1 回の shell 呼び出しでヒアドキュメントは 1 つまで**(HC-002)。ファイルを書いたら、
  次の呼び出しで結果を検査してから先へ進む。複合コマンドは途中で失敗しても後半が
  黙って実行される/されないため、部分適用に気づけない
- `looplog.py append` の必須フィールドは毎回 LOOP_LOG_SPEC を見て確認する(HC-002)
- **終了コードを見る必要があるコマンドをパイプに通さない**(HC-002)。`check | head` は
  パイプラインの終了コードが `head` の 0 になり、`&&` の連鎖が失敗をすり抜ける。
  差分ゲートやテストの検査は単独で実行し、結果を目視してから次へ進む
- **図は主張と同じ系列を描いていることをテストで縛る**(HC-006)。軸 ID や名前で系列を
  選ばず、主張ごとに別の系列名を持たせる。図に描いた件数と検定に使った件数が違うなら
  画面上で両方を明示する。2026-08-25 に、清音/濁音の主張の図が形態対を描いていて S1 を出した
- **人間が書き込むファイルをパイプラインの出力先にしない**(HC-005)。出力先にせざるを得ない
  なら、書き込み前に人手の入力有無を検査して上書きを拒否する。判定・curation の成果は
  `gold/` に恒久記録として退避する。2026-08-25 に再生成が人手判定を消して S1 を出した
- **ヒアドキュメント内の Python で改行・タブのエスケープを含む文字列を書かない**(HC-002)。
  実際の改行・タブに化けて SyntaxError になる。`chr(10)`/`chr(9)` を使うか行番号で置換する
- **育つデータにテストを直接結合させない**(HC-004)。判定表・辞書・語彙のように
  「増えることが仕事」のデータは、固定フィクスチャを注入口(`decisions_path` 等)で差し替えるか、
  変化に耐える不変量(包含関係・単調性・欠落の不在)で書く。ある瞬間の集合の等号や件数を
  焼き込むと、curation を進めただけでテストが落ちる
- **テスト関数名は日本語でよいが、語間は必ず `_` で繋ぎ半角空白を入れない**(HC-003)。
  空白は SyntaxError になり、そのファイルのテストが 1 件も走らない。収集エラーは TDD の赤と
  紛らわしいので、テストを書いたら最初に `pytest --collect-only` が通ることを確かめる
- **probe 由来の数値は本流実装で再現するまで「未再現」と明記する**(HC-001)。
  probe は使い捨てで本流と同じレビューを受けていない。実際、表記ゆれ統合の数値は
  probe の測定順の誤りで過小(606 語)のまま SPEC に載り、本流実装が再計算するまで
  気づかれなかった(正しくは 641 語)。SPEC §6 の参考実測値には必ず再現状況を付す

---

<!-- scaffold:block agents_core v1.12.0 -->
## 共通規律(scaffold 管理領域 — 手動編集禁止)

このセクションはスキャフォールド・レジストリが管理する。内容を変更したい場合は、
このファイルを直接編集せず、失敗ログ → HARNESS_CHANGELOG 起票 → レジストリ改訂 → `scaffoldctl update` の経路で行うこと。

### 7 段階ループプロトコル

| 段階 | 名称 | 完了条件 |
|---|---|---|
| 1 | 計画 | 対象の要求 ID を特定し、`loop_start` を記録した |
| 2 | 文脈読込 | SPEC.md / IMPLEMENTATION_GUIDE.md の該当箇所と、直近ループのログを読んだ。**この段階で扱うデータ・設定ファイルの実物を開いた**(索引・要約・記憶ではなく、本文・設定そのもの — HC-014) |
| 3 | テスト先行 | TEST_SPEC.md にトレースする失敗するテストを書き、赤を確認した。**各ケースの期待値の出所(SPEC の条項 / 実測 / 外部権威)を書いた**(HC-016) |
| 4 | 実装 | ファイル編集 2 回ごとにテストを実行し、赤のまま次の編集に進んでいない |
| 5 | 検証 | 全テスト合格 + 独立再計算(該当時)を確認した |
| 6 | 文書同期 | SPEC/docs と実装の乖離(SPEC-DRIFT)を解消し、生成ドキュメントを再生成した |
| 7 | 完了 | `stage_end` → `loop_end` の順に記録し(**`loop_end` はループの最終行**。後から追記しても LL-09 で拒否される — HC-013)、ループログ validate に合格し、専用コミットを積んだ |

### ループ可観測性

全ループは loop-observability の規律(LOOP_LOG_SPEC / FAILURE_TAXONOMY)に従い
`logs/loops/{loop_id}.jsonl` に記録する。失敗は気づいた瞬間に分類コード付きで記録する。
ツーストライク(LL-10)と S1 即時起票(LL-12)は本プロジェクトでも有効である。

**`looplog.py append` の出力を破棄してはならない**(`>/dev/null` 等)。記録は拒否されることがあり、
出力を捨てると「記録できていないこと」に気づけない。毎回、成否を目視で確認する(HC-013)。

### 外部データの採録(HC-012)

外部のコーパス・データセットから採録対象を選ぶとき:

- **メタデータからの推定で確定しない。** 題名・分類番号・ラベルは候補生成にのみ使い、
  本文・実物を見るまで `needs_review` を外さない
- **抽出器を書く前に件数オラクルを探す。** コーパス自身が持つ手がかり(題名の数詞・目次・凡例の記載・
  公表された総数)を先に探し、見つからなければ「オラクル無し」と SPEC に明記する

### 期待値の書き方(HC-016)

テストの期待値を**観測する前に定数で書いてはならない**。書いた定数を仕様だと思い込むと、
正しい実装・正しいデータの方が落ちる(VERIF-FALSE)。

- **SPEC の保証粒度を超えない。** 超える精度を要求したくなったら、先に SPEC 側を上げる。
  例: SPEC が「暦日の一致」しか保証していないのに「分の一致」を要求してはならない
- **件数・行数は定数で書かない。** 「集合が一致する」「取りこぼしが無い」という不変量で書く。
  外部データは重複・欠落を含むので、数はデータが動くたびに壊れる
- **やむを得ず定数で書くなら、実測日と実測値をコメントに残す。** 後から見て
  「誰かの仮定」か「その日の観測」かが区別できない期待値を残さない

### エスカレーション規範

以下の場合は作業を止め、`escalation` を記録してから人間に確認する:
仕様の複数解釈(SPEC-AMB 相当)/ スコープ外ファイルへの変更が必要になった /
破壊的操作(履歴改変・データ削除・強制 push)/ 同種の修正の 3 回目の失敗(PROC-LOOP)。

### コミット規約

Conventional Commits(feat/fix/test/docs/refactor/chore)。スキャフォールド更新は
`chore: scaffold vX.Y.Z` の専用コミットで行い、機能変更と混ぜない。
<!-- /scaffold:block agents_core -->
