# SPEC.md — onomato-atlas(音象アトラス)

<!-- scaffold template v1.12.0 から展開(2026-08-25)。以後このファイルはプロジェクトが育てる -->

根拠となる構想と先行検証は `docs/concept.md`。数値の出所はすべて同文書 §2 / §11 の実測にある。

---

## 1. 目的

近代日本語のオノマトペについて、**音韻素性(音側)と用法分布(意味側)を独立に測り、
両者が一致する範囲を可視化する**静的 Web アプリ。辞書引きアプリではない。

主張は一文で「**語の音の形は、その語がどんな文脈に出るかを予測する**」。
この主張は検証可能でなければならない。したがって座標を音韻素性から定義してはならない(§3)。

---

## 2. 用語

| 語 | 定義 |
|---|---|
| 音側 | 表記から決定論的に得る音韻素性。語頭子音・第2子音・母音列・形態型・濁音/半濁音/促音/撥音/長音 |
| 意味側 | 青空文庫コーパスにおける共起分布。窓 ±5 の内容語 lemma、軽動詞ストップリスト適用 |
| 形態型 | ABAB / ABっと / ABっ / ABり / ABん / ABーっ / ABー |
| 対比ベクトル | 語対の集合(例 ABAB↔ABり)の意味側差分の平均方向。**軸はこれとして定義する** |
| 語幹 | ABAB 型の前半 2 モーラ(`ころころ` → `ころ`) |

---

## 3. 設計上の絶対条件(最重要)

**F-00: 意味側の座標を音側の素性から計算してはならない。**

音側から座標を作ると、「形態バリアントは持続軸だけ動く」「濁音は重い方へ動く」は
**構成上の恒真式**になり、何も反証できない(`docs/concept-v1.md` の欠陥)。
意味側はコーパスからのみ構成し、音側は**検証・色分け・逆引き**にのみ用いる。

この条項に反する実装は、テストが通っても不合格とする。

---

## 4. 機能要求

### 4.1 コーパスと抽出(L1)

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 青空文庫から §4.1.1 の選定基準で作品を取得する。取得実績のある ango-atlas 方式を流用する | must |
| F-02 | 形態素境界(fugashi + unidic-lite)で語を切り出す。正規表現のみの抽出を禁止する | must |
| F-03 | POS フィルタ(副詞 / 形状詞 / 感動詞 / 名詞)を適用する | must |
| F-04 | **表記ゆれを統合する**(カタカナ → ひらがな。`キラキラ` → `きらきら`) | must |
| F-05 | ABAB 型の語彙は**人手 curation**で確定する。判定基準は §4.2 | must |
| F-06 | **変化形は語幹が ABAB 型として語彙内に独立に存在することを要件とする**(自動 curation) | must |
| F-06b | **名詞ガード**: 変化形の出現の過半が名詞なら自動採用せず `needs_review` に回す | must |
| F-07 | 全採否判断を理由付きで `vocab_manifest.json` に記録する | must |

#### 4.1.1 コーパス選定基準(2026-08-25 確定・escalation 経由)

索引 `list_person_all_extended_utf8.csv`(2026-08-17 取得・19,501 行)を母集団とし、
以下を順に適用する。**各段の残存件数を `corpus_manifest.json` に記録する。**

| 段 | 条件 | 残存(2026-08-25 実測) |
|---|---|---|
| 0 | 索引全行 | 19,501 行 / 異なり作品 17,839 |
| 1 | 役割フラグ = 著者 | 18,303 行 |
| 2 | 作品著作権フラグ = なし | — |
| 3 | テキストファイルURL あり | — |
| 4 | 原題なし(翻訳を除く。訳文の音象徴は原語の影響を受けうる) | 17,480 行 / 異なり作品 17,084 |
| 5 | 文字遣い種別 = **新字新仮名のみ** | — |
| 6 | 分類番号が **913(小説)/ 914(随筆)/ K91(児童)** のいずれかを含む | 6,966 行 / 異なり作品 6,905 |
| 7 | **著者あたり 20 作品を上限**とする | **2,212 行 / 異なり作品 2,200 / 330 著者** |

- 段 5 の根拠: 歴史的仮名遣いはオノマトペ表記にも及び(`やうやう` `さうさう`)、同一語が別語として
  数えられる。kokoro-graph で擬古文が過小評価される問題を実測済み
- 段 7 の根拠: 上限なしでは宮本百合子 778 作・小川未明 605 作の 2 名で全体の 20% を占め、
  語彙がその 2 名の癖を反映する
- **段 7 の作品選択方法**: 作品ID 昇順に並べ等間隔で cap 件を取る**系統抽出**(`selection.SAMPLING_METHOD`)。
  乱数を使わないので種は無い。先頭 cap 件では作品ID の若い作品に偏るため
- 段 7 の行数(2,212)と異なり作品数(2,200)の差 12 件は**共著**(同一作品IDに著者行が複数)。
  取得リストは作品ID で一意化する
- **一意化後の実取得(2026-08-25)**: 2,200 作品 / **328 著者** / **52,213,438 字**(中央値 8,719 字)。
  著者が 330 → 328 に減るのは、共著行しか持たない著者が一意化で落ちるため。
  底本名・入力者・取得元URL・取得日は 100% 充足、校正者は 2,193/2,200

### 4.2 curation 規範

- **採用**: 音・声・様態を模す語(`きらきら` `ばたばた` `そわそわ`)
- **除外**: 反復による強調・程度の副詞で模写性のない語(`とうとう` `なかなか` `わざわざ` `いよいよ` `もともと`)
- **除外の手がかり**: 漢字反復表記(`色々` `段々` `益々` `度々` `一々` `銘々` `各々` `元々`)が
  一般に用いられる語は非擬態語である可能性が高い。**手がかりであって判定規則ではない**
- 判断がつかない語は `needs_review` に出力し、**エージェントは独断で採否を決めない**(エスカレーション)

#### 名詞ガード(F-06b・2026-08-25 追加)

F-06 の自動規則だけでは、**常用名詞と同音の変化形が語幹の採用に便乗して入る**。
Q-01 の人手判定(7/100)で発覚した。

- 混入していたもの: カタカナ外来語 9 語(`バター` 389 / `メリー` 192 / `パリー` 58 /
  `カラー` 36 / `スター` 32 / `ブルー` 13 / `フリー` 8 ほか。すべて名詞 100%・カタカナ 100%)と、
  和語名詞(`うねり` `しおり` `ぼたん` `のこり` `もぐり` `ずきん` `じゃり` `じめん` ほか)。
  変化形 238 語中 **27 語・延べ 1,344**
- **品詞による自動除外は採れない。** unidic は「ちろりと彼女の顔を見た」(擬態語)と
  「ちろりを取りあげる」(酒器)を**どちらも名詞**にする。「がたりと鞭を投げ出して」(擬態語)と
  「炉辺の夜がたり」(語り)も同様。品詞で落とすと本物の擬態語まで消える
- したがって**除外ではなく保留**にする。名詞優勢の変化形は人手判断に戻す(AGENTS §2)
- 「過半」(0.5)は定義であって較正した閾値ではない。変えるときは本条項を先に直す

### 4.3 二チャネル(L2)

| ID | 要求 | 優先度 |
|---|---|---|
| F-10 | 音側: 語頭子音・第2子音・母音列・形態型・濁音/半濁音/促音/撥音/長音を抽出する。人手介入なし | must |
| F-11 | 意味側: 窓 ±5 の内容語 lemma 共起行列 → PPMI → SVD | must |
| F-12 | 係り先用言(後方窓 5 で最初の動詞/形容詞)を別特徴として保持する | should |
| F-13 | **軽動詞ストップリストは版管理する**。ヘッダに version を持ち、変更は `data: stoplist vX.Y — 理由` の専用コミット | must |
| F-14 | ストップリストを変更したループでは**感度分析**(変更前後の座標変位)を実行しループログに記録する | must |
| F-15 | SVD 次元数は較正(L3)で決める。**較正前に定数を書かない** | must |

### 4.4 軸の推定と採否(L3)

| ID | 要求 | 優先度 |
|---|---|---|
| F-20 | 軸は手で定義せず、**対比ベクトル**として語対集合から推定する | must |
| F-21 | 各軸候補に leave-one-out 方向一致検定を課し、**対照 2(対応シャッフル)を有意に上回るもののみ採用**する | must |
| F-22 | **検定を通っていない軸は UI に出さない**。スライダーも射影値も提供しない | must |
| F-23 | 採用軸・不採用軸とその検定結果を `axes.json` に記録し、UI 上で出典として参照できるようにする | must |

### 4.5 公開(L4)

| ID | 要求 | 優先度 |
|---|---|---|
| F-30 | **採用軸による 1 次元の並びを主に据える**。軸スコアは再現するが、空間内の個別位置は再現しない(§Q-02 の較正)。2D 散布図を出す場合、個別位置を主張せず**領域**として示し、再現性の低さを明示する | must |
| F-31 | 音側素性による色分けを重ねる。領域を作るか否かが見どころ | must |
| F-32 | 語ページ `/o/{語}` を静的生成する。用例(底本表記付き)・音側素性・**採用軸の射影値(密度下限を満たす語のみ)** | must |
| F-32b | **近傍語リストを UI に出さない。** 分割半での近傍一致率は @1 で 0.117 しかなく、個々の語の近傍は再現しない。出すなら再現性の数値を併記し「このコーパスでの近傍」と明示する | must |
| F-33 | 検索・逆引き(素性距離の最近傍)は**クライアント完結**。API を用いない | must |
| F-34 | 年代分布と語彙規模を UI に明示し、現代語への一般化を主張しない | must |

### 4.6 後続(L5–L7)

| ID | 要求 | 優先度 |
|---|---|---|
| F-40 | 作家別使用傾向。宮沢賢治の造語を既存語空間へ射影(O-5) | should |
| F-41 | 未登録音形の語釈生成(Route Handler → Claude API) | **スコープ外に落とす**(2026-08-25) |

#### F-41 を落とした理由(2026-08-25・escalation 経由)

生成した語釈は**何とも照合できない**。本アプリは全ページを「測ったものだけを出す」で
通してきたので、唯一検証不能な出力を並べると、他の記述の信頼も損なう。
加えて N-03 の静的配信を崩し、API 依存とコストが乗る。優先度は could だったため落とす。

**構想時の逆引き(文を入れて場面に合う語を出す)も出せない。** 語レベルの意味主張を
要求するが、それが再現しないことは Q-02 の較正で分かっている(近傍一致率 @1 = 0.117)。
代わりに、決定論的な音側素性と、群として検定を通った軸だけで絞る形にした(F-33)。
| F-42 | 語感クイズ + OGP 共有 | could |

---

## 5. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | パイプラインの外部依存は **fugashi + unidic-lite + numpy** のみ。係り受け解析器を導入しない | requirements の検査。追加が必要と判断したらエスカレーション |
| N-02 | 青空文庫への実アクセスは取得コマンドの手動実行のみ。テストはフィクスチャ駆動でネットワーク不要 | CI でネットワーク遮断してテストが通ること |
| N-03 | 実行時に形態素解析・コーパス走査を行わない。成果物 JSON をビルド前に生成しコミットする | Route Handler が v0.4 まで存在しないこと |
| N-04 | 音側素性抽出と距離計算を Python と TypeScript の**二重実装**とし、同一フィクスチャで完全一致 | O-4 |
| N-05 | 引用は**二層原則**。表示層は青空文庫本文と逐語一致、分析層は正規化済み。底本・入力者・校正者を必ず付す | O-6 |
| N-06 | ライセンス二層。コード MIT / 自作データ CC BY 4.0。既存の擬音語辞典から語釈・見出し語リストを持ち込まない | LICENSE-DATA.md の存在と内容 |
| N-07 | Vercel 静的 export。Root Directory = `web` | デプロイ成功 |

---

## 6. 品質基準

**較正ファースト。以下の閾値は L3 の較正で確定する。現時点で確定値を書かない。**

| ID | 基準 | 状態 |
|---|---|---|
| Q-01 | 抽出の偽陽性率(語境界フィルタ後、人手検査標本に対して)。**合格 = 95% 信頼上限が 10% 未満** | **確定**。偽陽性 2/100 = 2%(Wilson 95% CI 0.6–7.0%)→ **合格** |
| Q-02 | 軸スコアを表示できる密度下限。**軸ごとに定める**。基準 = 分割半信頼性(Spearman-Brown 補正)0.70 以上 | **確定**。duration ≥40(458 語・0.704)/ roughness ≥120(243 語・0.711) |

### Q-01 の測り方(2026-08-25 訂正)

Q-01 は**抽出の誤分割**を測る指標であり、curation の採否判断とは別物である。
検査標本は**採用語の出現のみ**から引く。全候補から引くと `いろいろ` `つもり` `やはり` のような
curation 済みの非擬態語が混ざり、抽出の誤りと採否の判断が混ざる(実際に 200 件中 146 件が混ざった)。

### Q-02 / Q-03 の較正結果(2026-08-25・L3 loop_014)

**方法**: 作品を偶奇で二分し、各半分から独立に埋め込みを作る(k=100・min_ctx_count=5)。
作品単位で割るのは、同一作品内の共起が相関するため。出現の無作為間引きではその相関を
無視して信頼性を過大評価する。

#### 較正で判明した最重要の事実 — 主張のレベルによって再現性が違う

| 主張のレベル | 指標 | 分割半での値 | SB 補正 | 出せるか |
|---|---|---|---|---|
| 群レベル(軸そのもの) | 対の平均差 | 前半 +0.147 / 後半 +0.130(duration) | — | **出せる** |
| 語レベル(軸スコア) | 射影値の相関 | 0.496(duration)/ 0.367(roughness) | 0.66 / 0.54 | **密度下限つきで出せる** |
| 語レベル(近傍リスト) | 近傍一致率 @1 | **0.117** | — | **出せない** |
| 語レベル(空間内の位置) | 類似度順位の Spearman | **0.123**(中央値) | 0.22 | **出せない** |

**個々の語の近傍リストは再現しない。** k を 10〜200、近傍数を 1〜20、`min_ctx_count` を
1〜50 で掃引しても、一致率は最良 0.170 までしか上がらなかった。
**意味場は保たれる**が(`きらきら` の近傍は両半分とも光の語)、**どの語が並ぶかは入れ替わる**。

一方、**軸そのものは頑健**である。軸は 202 組(duration)/ 32 組(roughness)の平均であり、
語ごとのノイズが相殺される。対の平均差は両半分で同符号・ほぼ同値だった。

#### Q-02 の確定(軸ごとの密度下限)

密度は**軸スコアの信頼性を単調に予測する**(近傍一致率とは違って):

| 密度帯 | 語数 | duration r | roughness r | duration SB |
|---|---|---|---|---|
| 0–20 | 41 | 0.119 | 0.115 | 0.212 |
| 20–40 | 93 | 0.293 | 0.231 | 0.454 |
| 40–80 | 123 | 0.336 | 0.251 | 0.503 |
| 80–160 | 162 | 0.523 | 0.396 | 0.687 |
| 160–320 | 92 | 0.610 | 0.443 | 0.758 |
| 320– | 81 | **0.799** | **0.697** | **0.888** |

SB 補正 0.70 を満たす最小の下限:

| 軸 | 密度下限 | 語数 | SB 補正信頼性 |
|---|---|---|---|
| duration(瞬間⇔持続) | **≥ 40** | 458 | 0.704 |
| roughness(軽柔⇔重粗) | **≥ 120** | 243 | 0.711 |

**下限未満の語には `axes.json` が射影値を持たない**(T-216 が検査)。UI 側の実装ミスで
出てしまう余地をデータ側で断つ。

#### Q-03 の確定(軸採用基準)

**方向一致の二項 p と、対応シャッフル対照の経験 p の両方が α=0.05 未満。**片方では採用しない。

実データでの効き目: `moraic_n`(ABAB → ABん)は二項 p = 1.56e-06 と極小だが、
対照の経験 p = 0.515 で**却下**された。素朴に二項検定だけを見ていれば採用していた軸である。
却下された軸は `axes.json` からベクトルも射影値も落とされる(T-213)。
| Q-03 | 軸採用の検定基準 | **確定**。方向一致の二項 p **と** 対応シャッフル対照の経験 p の**両方**が α=0.05 未満 |
| Q-04 | 引用の逐語一致率 = 100%(N-05) | 確定 |
| Q-05 | 二重実装の一致 = 完全一致(N-04) | 確定 |

### 参考実測値(2026-08-25・kokoro-graph bronze 317 作品 / 13,053,903 字)

**これは observation であって仕様ではない。**コーパス拡大後に必ず再測定する。

| 観測 | 値 | 再現状況 |
|---|---|---|
| 語境界フィルタで落ちる偽陽性 | 延べの 10.4% | probe 由来・**未再現** |
| ABAB 型 異なり語(頻度制限なし) | 774 語 | probe 由来・**未再現** |
| 頻度 ≥30 の語数 | 119 語 | probe 由来・**未再現** |
| 表記ゆれ統合後・頻度 ≥5 の異なり語 | **641 語** | **本流実装で再計算(2026-08-25)** |
| 意味側の異なり共起語 中央値 / 最小 | 120.5 / 19 | probe 由来・**未再現** |
| SVD 寄与率 d1 / d1-5 / d1-30 | 5.0% / 9.2% / 27.2% | probe 由来・**未再現** |

### 本コーパス(2,200 作品)の実測 — すべて本流実装由来(2026-08-25)

| 観測 | 値 |
|---|---|
| 取得 | 2,200 / 2,200 件・失敗 0 |
| 生テキスト / 正規化後 | 52,213,438 字 / **46,687,923 字** |
| 抽出 異なり語 / 延べ | **1,751 / 134,681** |
| 頻度 ≥5 の異なり語 | **1,094** |
| curation | 採用 **173** / 除外 **554** / 保留 **367** |
| 除外の内訳 | 語幹不在の変化形 488 + 反復強調の副詞 66 |
| 共起密度(採用 173 語の異なり共起語数) | 最小 **13** / 中央値 **168** / 最大 **1087** |
| 密度 <10 の語 | **0 件** |
| 判定表 / 軽動詞リスト | vocab_decisions **v1.1.0** / light_verbs v1.0.0 |

#### Q-01 の確定値(2026-08-25)

| 版 | 語彙 | 標本 | 偽陽性 | Wilson 95% CI | 記録 |
|---|---|---|---|---|---|
| v1 | 採用 629(F-06b 前) | seed 20260825 / 100 件 | 6/100 = **6%** | 2.8 – 12.5% | `gold/q01_judged_20260825.tsv` |
| v2 | 採用 597(F-06b 後) | seed 20260826 / 100 件 | **偽陽性 2/100** = **2%** | **0.6 – 7.0%** | `gold/q01_judged_20260826.tsv` |

**合格基準を「95% 信頼上限が 10% 未満」と定める**(較正後に確定)。v2 は 7.0% で合格。
再測定は**独立した標本**で行った。修正を導いた標本で測り直しても意味がないため。

v2 に残る 2 件はいずれも**複合語の内部から切り出した断片**である:
`ごんごん`(「ごんごんごま」= 独楽の名)と `パンパン`(「パンパンガール」)。
**外来語(モード A)は 0 件**で、F-06b 名詞ガードが効いたことを示す。
複合語内部の断片は形態素解析器自身の分割に起因し、現行の道具立て
(fugashi + unidic-lite、N-01)では自動検出できない。**残存として受け入れる。**

#### Q-01 v1 の内訳(2026-08-25・人手判定)

**偽陽性 6/100 = 6%**(Wilson 95% 信頼区間 **2.8% – 12.5%**、標本 seed 20260825・採用語限定)。
判定の恒久記録は `gold/q01_judged_20260825.tsv`。

内訳:

| モード | 件数 | 例 |
|---|---|---|
| A カタカナ外来語が変化形に化ける | 4 | `パリー`(Paris)/ `バター`(butter)×3 |
| B 語をまたいだ断片 | 1 | 「スッ**トントン**節」→ `トントン` |
| C 複合動詞の内部 | 1 | 「酔っ**ぱらっ**た」→ `ぱらっ` |

当初 `はははは` を含む 7 件としたが、確認の結果 **判定が撤回され 6 件**となった
(笑い声の表記であって誤分割ではない)。Q-01 は**抽出の誤分割**のみを測る指標であり、
擬態語として採るかどうかは curation の判断である、という線引きが確認された。

モード A は F-06b(名詞ガード)で塞いだ。**モード B / C は未対処**で、
形態素解析器自身の分割に起因するため、現行の道具立てでは自動検出できない。
再測定は**独立した標本**(seed 20260826)で行う。修正を導いた標本で測り直すと
数字が良く出るのは当然であり、意味がないため。

#### 意味側チャネルの実測(2026-08-25・本流実装由来・L2 loop_010)

| 観測 | 値 |
|---|---|
| 対象語 | 採用 597 語 |
| 共起行列 | 597 語 × **4,210 文脈語** / 非零率 **2.78%** |
| ストップリスト | light_verbs **v1.0.0**(15 語・版付きファイル) |
| SVD 寄与率 | d1 3.8% / d2 0.8% / d3 0.7% |
| 累積寄与率 | d1-10 9.1% / d1-30 17.1% / d1-50 23.8% / d1-100 38.2% |

**寄与率は低い**(行列が疎なため)。先行検証と同じく、
**低次元射影より高次元のコサイン近傍のほうが信頼できる**。地図の座標に
低次元 SVD 射影をそのまま使わない(F-30)。

近傍の例(音側を一切使わず、コーパス分布のみから):

```
きらきら → ぎらぎら(.62) ぎらっ(.60) きらり(.52) ちかちか(.49)   光
ぐるぐる → くるくる(.61) ぐるっ(.46) ぐるり(.35)                 回転
どきどき → どきん(.64) どきっ(.60) どきり(.58) わくわく(.48)     動悸・情動
ばたばた → ぱたぱた(.55) どかどか(.41) ばたり(.33)               音・動作
ざらざら → ぺろり(.43) べろり(.42) ぬめぬめ(.40) ねとねと(.39)   触感・食感
そわそわ → くさくさ(.42) きびきび(.39) がみがみ(.39)             心的態度
```

#### 判定表 v1.1.0 の感度分析(2026-08-25・AGENTS §3)

保留語の採用提案 322 語を人間が承認して判定表に追加した。変更前後:

| 指標 | v1.0.0 | v1.1.0 | 変位 |
|---|---|---|---|
| 採用語 | 173 | **629** | +456 |
| ABAB / 変化形 | 69 / 104 | 391 / 238 | +322 / +134 |
| O-1 パラダイム対 | 104 | **238** | +134 |
| O-2 濁音ミニマルペア | 14 | **39** | +25 |
| needs_review | 367 | 45 | −322 |
| 共起密度 最小 / 中央値 / 最大 | 13 / 168 / 1087 | 5 / 96 / 1087 | −8 / −72 / 0 |

- **O-2 の対が 2.8 倍**になった。先行検証で O-2 が通らなかった原因は検出力だったので、
  L3 の再検定はこの語彙で行う
- **密度の最小が 13 → 5 に下がった**。低頻度語(`ぜえぜえ` 頻 5 / 密 5、`わあん` 頻 7 / 密 5)が
  入ったため。密度 <10 は 4 語(0.6%)、<20 は 48 語(7.6%)。
  **Q-02 の下限をどこに置くかは L3 の較正で決める**
- O-2 の 39 組には偶然の同音対(`くすくす/ぐずぐず` `ごほごほ/ごぼごぼ` `もくもく/もぐもぐ`
  `もそもそ/もぞもぞ` `すらすら/ずらずら` `はりはり/ばりばり`)が混じる。
  **L3 では意味的に対応する対の人手選別が要る**(先行検証でも同じ問題が出た)

先行検証(13.05M 字)で「異なり共起語 最小 19 は薄すぎる」と書いた懸念は、
コーパスを 3.6 倍にしたことで解消した。密度が低いのは低頻度の変化形
(`べたん` 頻 5 / 密 13、`はたり` 頻 5 / 密 15)に限られる。

**probe 由来の数値は本流実装で再現するまで信用しない**(HC-001)。
`probes/` は使い捨てで本流と同じレビューを受けていない。実際、表記ゆれ統合の数値は
probe が「頻度で切ってから統合」の順で測っており 606 語と過小だった(正しい順では 641 語)。

---

## 7. オラクル

| ID | 内容 | 状態(2026-08-25 先行検証) |
|---|---|---|
| **O-1** | 形態パラダイム: ABAB → 変化形 の意味側差分が方向一致するか | **通過(本コーパス・2026-08-25)**。下表 |
| **O-2** | 濁音ミニマルペアの意味側差分が方向一致するか | **通過(本コーパス・2026-08-25)**。§O-2 の確定検定 |
| **O-3** | ブーバ/キキの予測方向を事前登録し意味側で確認 | **H1 通過 / H2 不通過**(2026-08-25)。§O-3 の実行結果 |
| **O-4** | 音側素性抽出・距離計算の Python / TypeScript 二重実装一致 | **通過**(2026-08-25)。`gold/phon_cross.json` の 136 対で完全一致。TypeScript 側は期待値を持たず Python の実測値を読む |
| **O-5** | 賢治造語を訓練から外し既存語空間へ射影 | **意味側では成立しない**(2026-08-25)。`かぷかぷ` `どっどど` `もかもか` は頻度 5 未満で語彙に入らず、閾値を下げても密度が足りず軸に載らない。音側素性は頻度に依存しないので「音の作り方において既存語の内側にあるか」なら答えられる。主張を音側へ移すかは未決 |
| **O-6** | 引用の逐語一致 | **通過**(2026-08-25)。用例 3,561 件すべてが原文ファイルの部分文字列であり、底本名・入力者が空でないことを validation テストで確認 |

### O-1 の確定検定(2026-08-25・本コーパス 597 語 × 100 次元)

| 対比 | n | 方向一致 | 平均 cos | 二項 p | 対照 z(一致率) | 対照 経験 p |
|---|---|---|---|---|---|---|
| ABAB → ABり | 92 | 88/92 | +0.143 | 5.9e-22 | **+3.64** | **0.000** |
| ABAB → ABっ | 63 | 54/63 | +0.143 | 3.05e-09 | +1.26 | 0.160 |
| ABAB → ABん | 45 | 38/45 | +0.131 | 1.56e-06 | +0.24 | 0.528 |
| **統合** | **202** | **184/202 (91%)** | **+0.156** | **3.87e-36** | **+2.29** | **0.0125** |

対照は**対応シャッフル**(B 側の割り当てだけを無作為化)。形態そのものが持つ共通方向を
保存するため、素の無作為対より厳しい。対照の一致率は 87%±2%、平均 cos +0.127±0.002。
平均 cos に対する z は **+14.52**。

**読み方**: 形態の共通方向は 3 形態すべてで強い(二項 p はいずれも極小)。
しかし**語ごとの対応**まで示せたのは **ABり だけ**で、ABっ・ABん は対照を超えない。
統合では対照を超える(経験 p 0.0125)が、一致率の z は +2.29 と控えめである。
**O-1 に事前登録は無い**ため、統計量を選ばずすべて記載する。

軸の両極(音側を一切使わず、意味側の座標だけから):

```
+極  ぷすり べたん ばらり ばたっ ぱらり どかん するっ ずばり どかっ ぺたっ べたっ ぱたり
-極  ぷりぷり びくびく ばたばた ぶるぶる かさかさ ふつふつ がたがた ごうごう ぶりぶり はあはあ
```

+極はすべて変化形(瞬間・一回完了)、−極はすべて ABAB(反復・継続)。
**構想が予測した「瞬間⇔持続」軸が、音韻素性を入力せずに再発見された。**

### O-2 の確定検定(2026-08-25・本コーパス)

対比語対は `data/curated/o2_pairs.tsv` **v1.0.0**(人手選別・**測定器として版管理**)。
39 組のうち 32 組を「清音側と濁音側が同じ事象を指し、濁音化だけが質感を変える真の対」と判定。

| 集合 | n | 方向一致 | 平均 cos | 二項 p | 対照 z | 対照 経験 p |
|---|---|---|---|---|---|---|
| **選別 32 組(主分析)** | 32 | 23/32 (72%) | +0.041 | **0.010** | **+3.18** | **0.000** |
| 全 39 組(選別なし) | 39 | 32/39 (82%) | +0.056 | 3.51e-05 | +4.65 | 0.000 |
| 偶然と判定した 7 組 | 7 | 7/7 | +0.114 | 0.0078 | +1.67 | 0.127 |

**先行検証(12 組・7/12・p=0.39)では通らなかった。**語彙を 173 → 597 に広げて
対を 12 → 32 組にしたことで通った。原因は検出力だったという見立てが実測で裏づけられた。

#### 想定と食い違った点(2 件・そのまま記載する)

1. **選別しない 39 組のほうが強い**(p=3.5e-05 対 0.010)。人手選別は信号を改善しなかった。
   主分析は事前に宣言したとおり選別 32 組を採る
2. **偶然と判定した 7 組も方向が揃う**(7/7・p=0.0078)。対応シャッフル対照は超えない
   (経験 p 0.127・n=7)。**濁音化の効果が、対の意味的対応を必要としないほど系統的**である
   可能性を示す。これは音象徴の主張としてはむしろ強い読み方だが、n=7 で結論はできない

#### 軸が対の外へ一般化する(非循環の確認)

軸の構成に**使っていない 533 語**で検証した:

| 群 | n | 軸上の平均 |
|---|---|---|
| 濁音を含む語 | 204 | **+0.0722** |
| 濁音を含まない語 | 290 | +0.0244 |

差 +0.0478・並べ替え検定 **p=0.0001**。軸の構成に使わなかった語でも、濁音の有無が
軸上の位置を予測する。

さらに **+極には清音の語も混じる**(`みしみし` `きしきし` `ぴたぴた`)。
軸が捉えているのは濁音そのものではなく、**濁音が予測する意味の次元(粗さ・硬さ・重さ)**である。

```
+極  ごとごと みしみし ばりばり めりめり がたがた きしきし ぎしぎし ごうごう ごみごみ ぎちぎち
-極  そよそよ ちらちら すうすう ぽたぽた ふわり ぱさぱさ うらうら ぽちぽち しらしら かちり
```

### O-3 の実行結果(2026-08-25・事前登録どおり)

事前登録 `gold/o3_preregistration.json`(sha256 封印・2026-08-25 登録)の手続きを
一切変えずに実行した。結果は `gold/o3_result_20260825.json`。

手続き: 並べ替え検定 10,000 回 / seed 20260825 / 片側 / α=0.05 / Holm 補正 / 最小群サイズ 10。

| 仮説 | 群 A | 群 B | 平均差 | 素の p | Holm 補正後 | 判定 |
|---|---|---|---|---|---|---|
| **H1** 無声破裂音(k/t/p)は共鳴音(m/n/r/w/∅)より「衝撃・打撃」プローブと共起する | n=169 平均 1.590 | n=82 平均 0.422 | **+1.169** | 0.0002 | **0.0004** | **通過** |
| **H2** 母音 i を含む語は含まない語より「鋭・細・小」プローブと共起する | n=173 平均 0.990 | n=381 平均 0.761 | +0.228 | 0.0646 | 0.0646 | **不通過** |

**読み方**:

- **子音側の音象徴は確認された。** 破裂音が衝撃・打撃の文脈に偏るという先行研究の予測方向が、
  近代日本語のオノマトペのコーパス分布で再現された
- **母音側は方向は合っていたが有意でない**(p=0.065)。α=0.05 をわずかに上回る。
  **閾値を動かさない。** 事前登録に α=0.05 と書いた以上、0.065 は不通過である
- H2 は Holm 補正で値が変わらない(2 仮説のうち大きい方の p は ×1 のため)

**事後の変更点(開示)**: 実行後に `run_all` の総合ラベルを二値(pass/fail)から
仮説ごとの集計に変えた。事前登録の決定規則は**仮説ごと**であり、総合ラベルは
コード側の勝手な発明だったため。**検定の手続き・パラメータ・判定は一切変えていない。**

### 件数オラクル(HC-012)

**コーパス側にはオラクルが存在する。** 青空文庫索引 CSV の行数がそれで、
選定の各段の残存件数(§4.1.1 の表)を突き合わせ可能な正解として使う。
取得結果の作品数が段 7 の件数と一致しない場合は、差分を manifest に列挙して説明する。

**語彙数の側にはオラクルが存在しない。**
擬音語辞典の見出し語数は権利上も方法論上も基準にできず(§N-06)、
「近代日本語のオノマトペは N 語である」という公表値も採用しない。
語彙数は**コーパスからの実測値 + curation 判断の記録**(F-07)としてのみ扱い、
テストで定数として固定しない(HC-016)。

---

## 8. データ契約

| 成果物 | 内容 |
|---|---|
| `vocab_manifest.json` | 語・頻度・形態型・採否・採否理由・needs_review フラグ |
| `phon.json` | 音側素性(決定論。語 → 素性辞書) |
| `sem.json` | 意味側ベクトル(SVD 30 次元)・近傍リスト・密度 |
| `axes.json` | 軸候補・対比語対・検定結果・採否 |
| `usage/{語}.json` | 用例(表示層逐語 + 底本表記)・作家別頻度 |
| `corpus_manifest.json` | 取得作品・作者・年代・文字数・除外判断 |

---

## 9. ループ計画

| ループ | 内容 | 完了条件 |
|---|---|---|
| L1 | コーパス拡大・抽出・curation | 1,000〜3,000 作品取得。F-01〜F-07。頻度分布を数で報告。**Q-01 は検査標本を用意(判定は人手)/ Q-02 は分布を実測** |

L1 は差分ゲート(500 行)に収まらず 4 ループに分割した:
loop_001 選定 / loop_002 抽出・curation / loop_003 本取得 / loop_004 語彙構築・品質指標。
| L2 | 二チャネル構築 | F-10〜F-15。ストップリスト v1.0。**O-3 の予測方向を事前登録** |
| L3 | 軸推定と較正 | F-20〜F-23。O-1 / O-2 / O-3 を再検定。**Q-02 / Q-03 確定**(Q-02 は L1 で分布のみ実測) |
| L4 | UI + 公開 | F-30〜F-34。Vercel 公開・app-menu 掲載 → **v0.1** |
| L5 | 用例層 | F-40。O-5 / O-6 |
| L6 | 逆引き + 生成 | F-33 の TS 実装・O-4。F-41 |
| L7 | クイズ + OGP | F-42 |

---

## 10. スコープ外

- **現代日本語コーパス**。青空文庫は明治〜昭和前期であり、本アプリは「近代日本語のオノマトペ」を扱う(F-34)
- **既存の擬音語辞典の語釈・見出し語リスト**(N-06)
- **音声・音響データ**。扱うのは表記から得られる音韻素性のみ
- **オノマトペの通時変化**。年代分布は表示するが通時分析はしない
- **日本語以外の言語との対照**。O-3 は先行研究の予測方向を借りるだけで、他言語コーパスは扱わない
- **係り受け解析**。先行検証で窓 3 以内に用言が 77.1% 到達することを確認済み(N-01)
- v0.4 までの**サーバー実行**(N-03)
