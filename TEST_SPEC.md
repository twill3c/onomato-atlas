# TEST_SPEC.md — onomato-atlas

<!-- scaffold template v1.12.0 から展開(2026-08-25) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない** |
| 外部権威 | 出典 URL と取得日をフィクスチャ先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

**本プロジェクト固有の注意**: 語彙数・抽出件数を定数で書かない(SPEC §7「件数オラクルの不在」)。
「集合が一致する」「取りこぼしが無い」「単調である」という不変量で書く。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `fixtures/boundary/` | 手作りの短文。`置いていて` `彼らはらはらと` `わからん` `…にやって` を含む | **偽陽性の既知例**。2026-08-25 の実測で正規表現のみが誤検出した実例(docs/concept.md §2 実測 2) |
| `fixtures/paradigm/` | 語幹 + 形態型の対(`ころころ/ころり/ころん`) | 音側抽出の決定論性を縛る。表記から一意に決まるので合成でよい |
| `fixtures/corpus_mini/` | 青空文庫 5 作品の抜粋(PD・底本表記付き) | 意味側パイプラインの端から端まで。**実データなので期待値は不変量で書く** |
| `fixtures/phon_cross/` | 音側素性の入出力対 | Python/TS 二重実装の一致(O-4)。両言語が同一ファイルを読む |
| 先行研究(O-3) | Köhler 1929 / Ramachandran & Hubbard 2001 の予測方向 | **事前登録ファイル** `gold/o3_preregistration.json`(2026-08-25 登録・sha256 で封印)。以後変更禁止。変更が要るなら新しい登録 ID を切り旧登録を残す |

## ケース一覧

### 抽出・curation(L1)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-02 | `置いていて` を含む文に抽出器をかける | `いていて` を語として返さない |
| T-002 | F-02 | `彼らはらはらと` を含む文 | `らはらは` を返さない |
| T-003 | F-02 | `わからん` `つまらん` を含む文 | `からん` を ABん 型として返さない |
| T-004 | F-02 | `…にやって` を含む文 | `にやっ` を ABっ 型として返さない |
| T-005 | F-04 | `キラキラ` と `きらきら` を含むコーパス | 統合後の集合に `きらきら` のみが現れ、頻度は両者の和 |
| T-006 | F-06 | 語幹 ABAB が語彙に無い変化形(`ひとり` `つもり` `つまり` `かなり` `やはり`) | 語彙に採用されない |
| T-007 | F-06 | 語幹 ABAB が語彙にある変化形(`きらきら` に対する `きらり`) | 語彙に採用される |
| T-008 | F-05 | curation 除外リストに載る語(`とうとう` `なかなか`) | 語彙に採用されない |
| T-009 | F-07 | 任意の語について | `vocab_manifest.json` に採否と理由が存在する。理由が空の語が 1 件も無い |
| T-010 | F-05 | 判定困難語 | `needs_review` に出力され、採用も除外もされていない |

### 二チャネル(L2)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-020 | **F-00** | 意味側の構成コードに音側モジュールの import が存在しないこと | 静的検査(AST)で import 0 件。**この検査に落ちたら他が全緑でも不合格**。対象は `tests/test_phon.py::SEMANTIC_MODULES` に列挙する |
| T-140 | F-10 | 語頭子音・第2子音・母音列 | `きらきら`→(k, r, ia) / `ぽつぽつ`→(p, t, ou) / `うろうろ`→あ行は子音なし |
| T-141 | F-10 | 濁音化 | 語幹の子音だけが変わり、母音列と形態型は変わらない |
| T-142 | F-10 | 決定論 | 同じ語に対して同じ素性 |
| T-143 | F-10 | 未対応の表記(拗音を含む 3 モーラ語幹) | 例外にせず form=unknown を返す |
| T-036 | O-3 | 事前登録の項目 | 仮説・プローブ語・群定義・検定手続き(検定法/反復数/種/片側/α/多重比較/最小群サイズ)・不通過時の扱いが揃う |
| T-037 | O-3 / F-00 | 事前登録の測定定義 | 測定は意味側のみで行い、音側素性は群分けにしか使わない |
| T-021 | F-10 | `ころころ` `ころっと` `ころり` `ころん` | 音側素性が語幹・母音で一致し、形態型のみ異なる |
| T-022 | F-10 | 濁音・半濁音・促音・撥音・長音を含む語 | 素性フラグが表記と一対一対応する |
| T-023 | F-11 | `fixtures/corpus_mini` | PPMI 行列が非負。行和 0 の語が語彙に残っていない |
| T-024 | F-13 | ストップリストファイル | ヘッダに version がある。version の無い辞書を読んだら例外 |
| T-025 | F-14 | ストップリストを変更した状態 | 感度分析の出力が存在し、ループログに記録がある |

### 軸(L3)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | F-21 | 既知の合成データ(方向が揃うよう作った差分ベクトル群) | 検定が通る。**前提(方向の一致度)をテスト内で assert して検算する** |
| T-031 | F-21 | 方向を無作為化した差分ベクトル群 | 検定が通らない。偽陽性を出さないこと |
| T-032 | F-21 | 対照 2(対応シャッフル)が実装されていること | シャッフル後の同方向率が実測を下回る |
| T-033 | F-22 | `axes.json` で不採用の軸 | UI 配信データに射影値が含まれない |
| T-034 | F-23 | 全軸候補 | 採否・検定統計量・対比語対が `axes.json` に記録されている |
| T-035 | O-3 | 事前登録ファイル | L2 コミット後に sha256 が変わっていない(事後変更の検出)。登録値は 2026-08-25 実測 |

### 公開(L4)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-040 | F-30 | 地図座標の生成 | 低次元 SVD 射影をそのまま座標に使っていない(近傍グラフ由来であること) |
| T-041 | F-32 | 語ページ | 用例に底本・入力者・校正者が付く(N-05) |
| T-042 | F-32 | 語ページ URL | 日本語 URL エンコードが往復一致。濁音の合成/結合文字ゆれで別ページに分裂しない |
| T-043 | F-33 | 逆引き | ネットワーク呼び出しを行わない |
| T-044 | F-34 | トップページ | 年代分布と語彙規模が表示され、「近代日本語」の但し書きがある |

### 検証(L5–L6)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-050 | O-6 / N-05 | 全用例 | 青空文庫本文と**逐語一致**。正規化済み文字列が引用として出力されていない |
| T-051 | O-4 / N-04 | `fixtures/phon_cross` | Python と TypeScript の出力が完全一致 |
| T-052 | O-5 | 賢治造語 | 訓練データに含まれていない(リーク検査)。射影結果は成否を問わず記録される |

## 未確定

以下は較正(L3)まで期待値を書かない。**書きたくなったら SPEC 側を先に上げる**(HC-016)。

- Q-01 偽陽性率の上限
- Q-02 意味側ベクトルの最小密度
- Q-03 軸採用の検定基準(z / p)
- SVD 次元数(F-15)
