# HARNESS_CHANGELOG.md — onomato-atlas ハーネス改訂台帳

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

---

## HC-001

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-25 |
| トリガー | `GEN-LOGIC` × 2(loop_001 stage 4 / loop_002 stage 5) |
| 診断 | 2 件目が本質的。先行検証スクリプト(`probes/`)が「頻度で切ってから表記ゆれ統合」の順で測っており、統合後 606 語という数値が過小だった(正しい順では 641 語)。この誤った数値がそのまま SPEC §6 の「参考実測値」に載り、本流実装が再現するまで誰も気づかなかった。**probe は使い捨てで、本流と同じレビューを受けていない**のに、そこから出た数値が仕様文書に昇格する経路があった |
| 改訂 | AGENTS.md プロジェクト固有規律 §8 に「**probe 由来の数値は本流実装で再現するまで『未再現』と明記する**」を追加。SPEC §6 の参考実測値表に「再現状況」列を設け、本流で再計算した数値と probe 由来の数値を区別する |
| 種別 | agents_md + spec |
| 効果検証 | 以後のループで、SPEC に載る実測値がすべて本流実装由来か「未再現」明記のどちらかであること。probe 由来の未再現値が無印で載っていたら再発 |
| propagation | onomato-atlas ✅(本改訂)/ フリート全体 ⬜ — **この教訓は probe → SPEC の経路を持つ全プロジェクトに一般化する。harness-kit の agents_core への昇格を提案するが、他プロジェクトのファイル変更は本ループのスコープ外なので人間の判断を仰ぐ** |
| 状態 | Open |

### 1 件目(参考)

`GEN-LOGIC` の 1 件目は loop_001 stage 4 のテスト関数名に半角空白を含めた SyntaxError。
自己修正で解決しており、ハーネス側の改善余地は小さいと判断した。起票の主眼は 2 件目にある。

---

## HC-002

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-25 |
| トリガー | `TOOL-MISUSE` × 2(loop_001 stage 7 / loop_002 stage 6) |
| 診断 | 2 件とも「1 回の呼び出しに詰め込みすぎた」ことに起因する。1 件目は `looplog.py append` に `result=` を渡した引数誤り(正しくは `outcome=`)。2 件目は複数のヒアドキュメントを 1 つの複合コマンドに連結した際、入力を伴わない `cat >> AGENTS.md.tmp` が残って stdin 待ちで 120 秒タイムアウトし、後続の AGENTS.md 更新が適用されないまま先へ進みかけた。**複合コマンドは失敗が途中で起きても後半が黙って実行される/されないため、部分適用に気づきにくい** |
| 改訂 | AGENTS.md プロジェクト固有規律 §8 に「**1 回の shell 呼び出しでヒアドキュメントは 1 つまで。ファイルを書いたら次の呼び出しで結果を検査してから次へ進む**」を追加。あわせて looplog の必須フィールドは毎回 LOOP_LOG_SPEC を見て確認する |
| 種別 | agents_md |
| 追記(2026-08-25・3 件目) | 差分ゲート検査を `wtctl check \| head -3` とパイプしたため、パイプラインの終了コードが `head` の 0 になり、**ゲートが赤(876 行 > 500)のまま `&&` でコミットが通った**。検査を通したつもりで通していない。根は同じ「複合シェル構文が失敗を隠す」で、改訂に「**終了コードを見る必要があるコマンドをパイプに通さない。検査コマンドは単独で実行し、結果を目視してから次へ進む**」を追加する |
| 追記(2026-08-25・4 件目) | ヒアドキュメント経由で Python を書き込む際、文字列中の `
` `	` が実際の改行・タブに化けて SyntaxError。改訂に「**ヒアドキュメント内の Python で改行・タブのエスケープを含む文字列を書かない**。`chr(10)`/`chr(9)` を使うか、対象を行番号で置換する」を追加 |
| 効果検証 | 以後のループで TOOL-MISUSE の再発 0 件。特に「書いたはずのファイルが更新されていない」「検査を通したつもりで通していない」種の失敗が出ないこと |
| propagation | onomato-atlas ✅(本改訂)/ フリート全体 ⬜ — 一般性が高いので harness-kit への昇格を提案する(人間の判断を仰ぐ) |
| 状態 | Open |

---

## HC-003

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-25 |
| トリガー | `GEN-LOGIC`(テスト関数名の空白)が loop_001 / loop_006 で再発。HC-001 で「1 件目は改善余地が小さい」と判断したのは**誤り**だった |
| 診断 | 日本語でテスト関数名を書くとき、自然な日本語として読点や語間の空白を入れたくなる。Python の識別子に空白は使えないため SyntaxError になり、collection 段階で**そのファイルのテストが 1 件も走らない**。赤の内容が「テストの失敗」ではなく「収集エラー」なので、TDD の赤と紛らわしい |
| 改訂 | AGENTS.md プロジェクト固有規律 §8 に「**テスト関数名は日本語でよいが、語間は必ず `_` で繋ぎ半角空白を入れない。書いたら `pytest --collect-only` が通ることを最初に確かめる**」を追加 |
| 種別 | agents_md |
| 効果検証 | 以後のループで collection エラー 0 件。再発したら規律ではなく仕組み(命名を機械検査する lint)へ格上げする |
| propagation | onomato-atlas ✅(本改訂)/ フリート全体 ⬜ — 日本語でテストを書く全プロジェクトに一般化しうる |
| 状態 | Open |

### HC-001 の訂正

HC-001 の「1 件目(参考)」で「自己修正で解決しており、ハーネス側の改善余地は小さいと判断した」と
書いたが、**同じ失敗が再発したのでこの判断は誤りだった**。軽微に見える自己修正可能な失敗でも、
再発するなら仕組みで防ぐ対象である。HC-001 の記述はそのまま残し、本エントリで訂正する。

---

## HC-004

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-25 |
| トリガー | `VERIF-FALSE` × 2(loop_006 stage 4 / stage 6) |
| 診断 | 2 件とも根は同じで、**テストが可変の本番データに結合していた**。(1) T-012/T-134 が本番の判定表を直接読み、`ぬめぬめ` を「未知語」の例に使っていたため、その語が採用に変わると落ちた。(2) T-150 が「提案シートの語集合 == needs_review」と等号で書かれ、承認で保留が 367→45 に減った瞬間に落ちた。**どちらもプロダクト側は正しく、テストが正しい変更を不合格にした**。curation・辞書・判定表のように「育つことが仕事」のデータに対して、ある瞬間の状態を期待値に焼き込んだのが原因 |
| 改訂 | AGENTS.md プロジェクト固有規律 §8 に「**育つデータ(判定表・辞書・語彙)にテストを直接結合させない**。(a) 固定フィクスチャを注入口(`decisions_path` 等)で差し替える、(b) 期待値は変化に耐える不変量(包含関係・単調性・欠落の不在)で書く。ある瞬間の集合の等号や件数を焼き込まない」を追加 |
| 種別 | agents_md |
| 効果検証 | 以後のループで、curation を進めた結果テストが落ちる事象が 0 件。落ちたらそれは仕様変更の検出であって、データ増加の巻き添えではないこと |
| propagation | onomato-atlas ✅(本改訂)/ フリート全体 ⬜ — 辞書やコーパスが育つ全プロジェクトに一般化しうる。scaffold の TEST_SPEC テンプレートに載せる価値がある |
| 状態 | Open |

---

## HC-005

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-25 |
| トリガー | `TOOL-MISUSE` **S1**(LL-12 により即時起票) |
| 診断 | `build_vocab` の再生成が、**人手判定を書き込んだ `q01_sample.tsv` を無警告で上書き**した。判定はコミット前だったため git にも残らず、人間の作業成果を失った。今回はコミット済みの未判定版と会話の記録から復元できたが、**復元できない設計だった**。パイプラインの出力先と人間の入力先が同じファイルだったのが根本原因 |
| 改訂 | (1) `build_vocab.write_sample()` を新設し、**既存ファイルに人手判定があれば `JudgedSampleExists` を上げて上書きしない**(T-170〜T-172)。(2) 判定済みの標本は `gold/` に**恒久記録**として退避する(`gold/q01_judged_20260825.tsv`)。(3) AGENTS.md §8 に「**人間が書き込むファイルをパイプラインの出力先にしない。出力先にせざるを得ないなら、書き込み前に人手の入力有無を検査する**」を追加 |
| 種別 | tooling + agents_md |
| 効果検証 | 以後、人手判定・人手 curation の成果が再生成で失われる事象が 0 件。`gold/` の判定記録がすべての測定値の裏付けとして残っていること |
| propagation | onomato-atlas ✅(本改訂)/ フリート全体 ⬜ — 人手 curation を持つ全プロジェクトに一般化する価値が高い |
| 状態 | Open |

---

## HC-006

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-25 |
| トリガー | `GEN-LOGIC` **S1**(LL-12 により即時起票) |
| 診断 | トップ画面の 2 つ目の図が、**本文の主張と別のデータを描いていた**。本文は「清音と濁音だけが違う対を 32 組選んで測った」と書いているのに、図は形態パラダイム対 56 組を roughness 軸に描き、同方向率 61%(ほぼ偶然の水準)を示していた。原因は配信データ生成が全採用軸に対して形態対だけを作り、画面側が軸 ID で系列を選んでいたこと。**系列が「どの主張のためのものか」を持っていなかった**ため、取り違えても誰も気づかない。テストは全緑、ビルドも成功していた |
| 改訂 | (1) 配信データで**形態対と濁音対を別の系列に分ける**(`paradigms` / `voiced_pairs`)。形態対は duration 軸にだけ生成する。(2) T-237 / T-238 で系列の取り違えを検査する。(3) AGENTS.md §8 に「**図は主張と同じ系列を描いていることをテストで縛る。**軸 ID や名前で系列を選ばず、主張ごとに別の系列名を持たせる」を追加 |
| 種別 | tooling + agents_md |
| 効果検証 | 以後、図の系列と本文の主張が食い違う事象が 0 件。図に描いた件数と検定に使った件数が違う場合は、画面上で両方を明示していること |
| propagation | onomato-atlas ✅(本改訂)/ フリート全体 ⬜ — 可視化を持つ全プロジェクトに一般化しうる |
| 状態 | Open |

**この失敗の重さ**: 読者に誤った証拠を見せる状態だった。テストが全緑でビルドも通っており、
**目視でしか見つからなかった**。用例の改行欠陥(loop_015)と同じ性質で、
「テストが通っていること」は「正しいものを見せていること」を保証しない。
